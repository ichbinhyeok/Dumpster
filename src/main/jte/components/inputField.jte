@param String id
@param String label
@param String type
@param String value
@param String placeholder
@param String classes
<div class="ui-field ${classes}">
    <label class="ui-field__label" for="${id}">${label}</label>
    <input class="ui-input" id="${id}" type="${type}" value="${value}" placeholder="${placeholder}">
</div>
